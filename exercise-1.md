### Question

What's the final width of a container that is 100px wide and has 5px of padding?

### Answer

It depends on the the box model configured for the container. If `box-sizing` is `content-box` (default) then final width remains 100px wide.